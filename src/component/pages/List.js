import React, {  useEffect } from "react";
import styled from "styled-components";

// style
const StyledTotal = styled.div``;

export default props => {

  useEffect(() => {});

  return <StyledTotal>total</StyledTotal>;
};
